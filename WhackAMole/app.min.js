const square=document.querySelectorAll(".square"),mole=document.querySelectorAll(".mole"),timeleft=document.querySelector("#time-left");let score=document.querySelector("#score"),result=0,currentTime=timeleft.textContent;function randomSquare(){square.forEach(e=>{e.classList.remove("mole")});let e=square[Math.floor(9*Math.random())];e.classList.add("mole"),hitPosition=e.id}function moveMole(){let e=null;e=setInterval(randomSquare,1e3)}function countDown(){currentTime--,timeleft.textContent=currentTime,0===currentTime&&(clearInterval(timerId),alert("GAME OVER ! Your final score is "+result))}square.forEach(e=>{e.addEventListener("mouseup",()=>{e.id===hitPosition&&(result+=1,score.textContent=result)})}),moveMole();let timerId=setInterval(countDown,1e3);